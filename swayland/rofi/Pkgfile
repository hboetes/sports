# Description: rofi clone with wayland support
# URL:         https://github.com/lbonn/rofi.git

name=rofi
version=git
release=1
branch=wayland
source=(https://github.com/lbonn/rofi.git)

build()
{
    cd $name
    meson_build -Dxcb=disabled
}
