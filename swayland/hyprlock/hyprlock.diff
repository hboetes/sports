diff --git a/src/core/hyprlock.cpp b/src/core/hyprlock.cpp
index f8ef4c5..15ae548 100644
--- a/src/core/hyprlock.cpp
+++ b/src/core/hyprlock.cpp
@@ -521,21 +521,22 @@ void CHyprlock::run() {
     m_sWaylandState = {};
     dma             = {};
 
-    m_vOutputs.clear();
-    g_pEGL.reset();
+    // Proper cleanup order to avoid segfaults:
+    // 1. Terminate threads and background resources that may use Wayland/EGL objects
+    g_pAuth->terminate();
+    pthread_kill(pollThr.native_handle(), SIGRTMIN);
+    pollThr.join();
+    timersThr.join();
+
+    // 2. Clean up renderer and EGL before disconnecting Wayland
     g_pRenderer.reset();
+    g_pEGL.reset();
     g_pSeatManager.reset();
+    m_vOutputs.clear();
 
+    // 3. Disconnect Wayland display last
     wl_display_disconnect(DPY);
 
-    pthread_kill(pollThr.native_handle(), SIGRTMIN);
-
-    g_pAuth->terminate();
-
-    // wait for threads to exit cleanly to avoid a coredump
-    pollThr.join();
-    timersThr.join();
-
     Debug::log(LOG, "Reached the end, exiting");
 }
 
