  * Check code for the TODO keyword.
  * show what sudo does, in yellow or so.
  * create a self-contained uninstaller script, so you won't need pkgutils at all to use its packages.
  * gradual cleanup options - ie only old source packages, only source packages, only packages, etc.
  * Äfoo.go etc don't get deleted
  * git rev-parse --short  HEAD
  * New files in /etc/ don't get installed, because they are rejected and since rejmerge can't find them in /etc they are deleted.
  %  rm -rf /usr/pkgmk/source/rustdesk-server
  * approved post install scripts

* put source in target dir, for easy pkgmk -c
