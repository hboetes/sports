  * Check code for the TODO keyword.
  * show what sudo does, in yellow or so.
  * create a self-contained uninstaller script, so you won't need pkgutils at all to use its packages.
  * gradual cleanup options - ie only old source packages, only source packages, only packages, etc.
  * Fix that stupid tar output about removing /
